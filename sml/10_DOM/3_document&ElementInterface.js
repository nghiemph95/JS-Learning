/** Mục đích tìm hiểu:
 * 1. Document node
 * 2. Ké thừa node là element (Element Interface)
 */

/** Docuemnt: Để truy cập đến node gốc trong cây DOM
 * document.documentElement
 * document.doby/head/cookie
 * ...
 */

/** chỉ có thể lấy getElementById khi sử dụng document */

/** ELement
 * Properties:
 * 1. element.className
 * 2. element.classList
 * 3. element.innerHTML
 * 4. element.outerHTML
 *
 * Element size:
 * 1. element.clientHeight
 * 2. element.clientTop
 * 3. element.clientWidth
 *
 * Scroll:
 * 1. element.scrollTop
 * 2. element.scrollBot
 *
 * Duyệt (Traversal):
 * 1. element.children
 * 2. element.firstElementChild
 * 3. element.lastElementChild
 *
 * Hàm (Methods):
 * 1. element.append(...nodes)
 * 2. element.remove()
 * 3. element.replaceWith(...nodes)
 * 4. element.before/after (add trước/sau 1 phần tử nào đó)
 *
 * Query element (Không có getElementById)
 * 1. element.getElementByClassName(name)
 * 2. element.getElementByTagName(name)
 * 3. element.querySelector(selector)
 */

/** HTMLELement interface
 * Properties:
 * 1. HTMLELement.title
 * 2. HTMLElement.tabIndex (tab tab liên tục)
 * 3. HTMLElement.innerText
 *
 * Methods:
 * 1. HTMLElement.focus() Con trỏ chuột tự focus
 * 2. HTMLElement.blur()
 *
 */
