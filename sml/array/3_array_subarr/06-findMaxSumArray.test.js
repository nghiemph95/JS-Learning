import { findMaxSumArrayV1 } from "./06-findMaxSumArray";

describe("Name of the group", () => {
  test("should ", () => {
    expect(findMaxSumArrayV1([])).toStrictEqual([]);
    expect(findMaxSumArrayV1([1, 2, 3, 0, 2, 4])).toStrictEqual([1, 2, 3]);
  });
});
