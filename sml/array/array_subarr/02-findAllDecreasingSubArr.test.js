import { findAllDecreasingSubArr } from "./02-findAllDecreasingSubArr";

describe("Name of the group", () => {
  test("should ", () => {
    expect(findAllDecreasingSubArr([3, 2, 1, 15, 10, 20])).toStrictEqual([
      [3, 2, 1],
    ]);
  });
});
