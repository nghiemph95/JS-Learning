import { findLastNegativeOdd } from "./04-findLastNegativeOdd";

describe("Name of the group", () => {
  test("should ", () => {
    expect(findLastNegativeOdd([-1, -3, -5])).toBe(-5);
    expect(findLastNegativeOdd([1, 3, 5])).toBe();
  });
});
