import { findMissingNumber } from "./02-findMissingNumber";

describe("Name of the group", () => {
  test("should ", () => {
    expect(findMissingNumber([5, 6, 8, 9, 10], 10)).toBe(7);
    expect(findMissingNumber([5], 6)).toBe(6);
  });
});
