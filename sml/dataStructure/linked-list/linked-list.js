/** Linked List : Danh sách liên kết
 * 1 phần tử gồm:
 * - value (data)
 * - pointer (tham chiếu tới phần tử tiếp theo)
 * - phần tử đầu tiên: Head
 * - phần tử cuối cùng: Tail
 */

/** có 3 loại linked list
 * 1. Singly 
 * 2. Doubly (2 chiều)
 * 3. Circular
 */

/** Việc thêm/xoá ở linked list rất đơn giản và tối ưu hơn Array */

/** Khi nào cần dùng linked-list
 * 1. Khi tạo 1 mảng dynamic size
 * 2. Truy cập: Đi qua từng bước
 * 3. Search: Linear (đi từng cái)
 * 4. Add/remove: Đơn giản và không phải re-allocate items
 * 5. Duyệt mảng (Traversal): Duyệt từ đầu đến cuối
 */